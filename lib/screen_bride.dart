import 'package:flutter/material.dart';

class ScreenBride extends StatelessWidget {
  const ScreenBride({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
